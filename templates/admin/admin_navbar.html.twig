<nav class="navbar navbar-expand-lg ">
    <div class="container-fluid">
        <div class="navbar-wrapper">
            <div class="navbar-minimize">
                <button id="minimizeSidebar" class="btn btn-warning btn-fill btn-round btn-icon d-none d-lg-block">
                    <i class="fa fa-ellipsis-v visible-on-sidebar-regular"></i>
                    <i class="fa fa-navicon visible-on-sidebar-mini"></i>
                </button>
            </div>
            <div class="navbar-brand">
                {% apply spaceless %}
                    {% if title is defined %}
                        {{ title }}
                    {% else %}
                        Administration
                    {% endif %}
                {% endapply %}
            </div>
        </div>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-bar burger-lines"></span>
            <span class="navbar-toggler-bar burger-lines"></span>
            <span class="navbar-toggler-bar burger-lines"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('accueil') }}" class="nav-link">
                        <i class="nc-icon nc-simple-remove" data-toggle="tooltip" title="Retour au site"></i>
                        <span class="d-lg-none">Retour au site</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('deconnexion') }}" class="nav-link text-danger">
                        <i class="nc-icon nc-button-power" data-toggle="tooltip" title="Déconnexion"></i>
                        <span class="d-lg-none">Déconnexion</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
